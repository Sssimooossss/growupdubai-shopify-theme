{%- if section.settings.message != blank -%}
  <div class="announcement-bar color-{{ section.settings.color_scheme }} gradient" role="region" aria-label="{{ 'sections.header.announcement' | t }}">
    <div class="page-width">
      <div class="announcement-bar__message">
        <h2 class="announcement-bar__text">
          {%- if section.settings.link != blank -%}
            <a href="{{ section.settings.link }}" class="announcement-bar__link link link--text focus-inset animate-arrow">
              {{ section.settings.message | escape }}
              <span class="icon-wrap">→</span>
            </a>
          {%- else -%}
            {{ section.settings.message | escape }}
          {%- endif -%}
        </h2>
      </div>
    </div>
  </div>
{%- endif -%}

<style>
.announcement-bar {
  padding: 0.75rem 0;
  background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
  color: white;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.announcement-bar::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
  animation: shimmer 3s infinite;
}

.page-width {
  max-width: var(--page-width);
  margin: 0 auto;
  padding: 0 var(--container-padding);
}

.announcement-bar__message {
  position: relative;
  z-index: 1;
}

.announcement-bar__text {
  margin: 0;
  font-size: 0.9rem;
  font-weight: 600;
}

.announcement-bar__link {
  color: inherit;
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
}

.announcement-bar__link:hover {
  opacity: 0.8;
}

.icon-wrap {
  transition: transform 0.3s ease;
}

.announcement-bar__link:hover .icon-wrap {
  transform: translateX(3px);
}

@keyframes shimmer {
  0% { left: -100%; }
  100% { left: 100%; }
}

@media (max-width: 768px) {
  .announcement-bar__text {
    font-size: 0.8rem;
  }
}
</style>

{% schema %}
{
  "name": "Announcement bar",
  "settings": [
    {
      "type": "text",
      "id": "message",
      "default": "🇦🇪 Free shipping to UAE & GCC countries! Real Arab followers guaranteed.",
      "label": "Announcement message"
    },
    {
      "type": "url",
      "id": "link",
      "label": "Announcement link"
    },
    {
      "type": "select",
      "id": "color_scheme",
      "label": "Color scheme",
      "options": [
        {
          "value": "primary",
          "label": "Primary"
        },
        {
          "value": "secondary", 
          "label": "Secondary"
        },
        {
          "value": "accent",
          "label": "Accent"
        }
      ],
      "default": "primary"
    }
  ],
  "presets": [
    {
      "name": "Announcement bar"
    }
  ]
}
{% endschema %}